#!/bin/bash

# Verifica se foi fornecido um argumento
if [ "$#" -eq 0 ]; then
    echo "Erro: Nenhum argumento fornecido."
    exit 1
fi

# Atribui o primeiro argumento a uma variável
input_file="$1"

cat "$input_file" | ./../parser/v7_parser.py -gm1 | ./../escape_rooms/v4python/game.py